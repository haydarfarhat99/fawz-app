# Dev: use the relative path to go through the Vite proxy (avoids CORS).
# Prod: replace with the absolute URL for your deployment.
VITE_API_BASE_URL=/api/v0

# Upstream the Vite dev proxy forwards /api/* to. Only used in `npm run dev`.
VITE_API_UPSTREAM=https://fawz-sandbox.dev.iqarx.com

VITE_APP_NAME=FAWZ
VITE_ENVIRONMENT=development
VITE_TEST_EMAIL=normaluser.20260506@fawz.com
VITE_TEST_PASSWORD=NormalUser@2026!
